/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/dalat2026',
  assetPrefix: '/dalat2026',
};

export default nextConfig;
